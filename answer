class CashRegister
  attr_accessor :total , :discount

def initialize(discount=0) #ptionally takes an employee discount which is why we have set to 0
    @total = 0
    @discount = discount 
    @items = [] #CashRegister #items returns an array containing all items that have been added
  end

def add_item(title, amount, quantity=1) ##add_item also accepts an optional quantity which is why we have it set to 1
end

def apply_discount
end  
